<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sports Booking System</title>

  <link rel="stylesheet" href="/css/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <style>
    body {
      font-family: 'Helvetica Neue', Arial, sans-serif;
      margin: 0;
      padding: 0;
      color: #ffffff;
    }

    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: url('/images/sports-bg.jpg') no-repeat center center fixed;
      background-size: cover;
      z-index: -1;
      filter: brightness(40%); /* Darken the background */
    }

    /* Container styling */
    .container {
      max-width: 600px;
      margin: 100px auto;
      padding: 40px;
      background: rgba(0, 0, 139, 0.85); /* Royal navy blue with transparency */
      border-radius: 12px;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
      text-align: center;
    }

    /* Heading Styling */
    h1 {
      font-size: 2.5rem;
      color: #ffffff;
      font-weight: bold;
      margin-bottom: 15px;
    }

    /* Subtitle Styling */
    p {
      font-size: 1.2rem;
      color: #e0e0e0;
      margin-bottom: 30px;
    }

    /* Form Label Styling */
    label {
      font-weight: bold;
      font-size: 1.3rem;
      margin-bottom: 10px;
      display: block;
      color: #ffffff;
    }

    /* Custom Dropdown (Select Field) */
    .custom-select-wrapper {
      position: relative;
      width: 100%;
      margin-bottom: 20px;
    }

    select {
      width: 100%;
      padding: 12px;
      padding-right: 40px;  /* Adjust space for the custom arrow */
      font-size: 1.1rem;
      font-weight: bold;  /* Make text bold */
      border: 2px solid #ccc;
      border-radius: 8px;
      background-color: #f0f0f0;
      color: #000;
      appearance: none;  /* Remove default arrow */
      outline: none;
      transition: border-color 0.3s ease;
    }

    select:focus {
      border-color: #ffffff;
    }

    /* Custom arrow styling */
    .custom-select-wrapper::after {
      content: "\25BC";  /* Unicode for down arrow */
      position: absolute;
      top: 50%;
      right: 15px;
      transform: translateY(-50%);
      pointer-events: none;
      font-size: 1.2rem;
      color: #333; /* Custom arrow color */
    }

    /* Button Styling */
    .btn {
      display: block;
      width: 100%;
      padding: 12px;
      background: #000080; 
      border: none;
      border-radius: 8px;
      color: #fff;
      font-size: 1.2rem;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    .btn:hover {
      background: #1b1b8f;
    }
    .footer-text {
      margin-top: 30px;
      font-size: 0.9rem;
      color: #ffffff;
      text-align: center;
    }

  </style>
</head>
<body>
  <div class="container">
    <h1>Sports Booking System</h1>
    <p>Select a center from the list below to view schedules and make bookings:</p>
    <form action="/select-center" method="POST">
      <div class="form-group">
        <label for="center">Choose a Center:</label>
        <div class="custom-select-wrapper">
          <select name="centerID" id="center" required>
            <option value="" disabled selected>Select a center</option>
            <% centers.forEach(center => { %>
              <option value="<%= center._id %>"><%= center.name %></option>
            <% }); %>
          </select>
        </div>
      </div>
      <button type="submit" class="btn">View Schedule</button>
    </form>
    <div class="footer-text">
      <p>&copy; HimaBindu Jadhav - IIB2021044 </p>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>